# Backend API and Auth (Next.js) – use your laptop's LAN IP for Expo Go on a physical device
EXPO_PUBLIC_API_URL=http://192.168.1.32:3030
EXPO_PUBLIC_AUTH_URL=http://192.168.1.32:3000

# Required for Expo Go on device:
# 1. Replace 192.168.1.32 with your laptop's LAN IP (same WiFi as phone).
# 2. Copy this to .env (or rename), then restart Expo: pnpm --filter mobile start
# 3. Run web on 3000 and api-server on 3030; they listen on all interfaces so the phone can connect.
# 4. Ensure firewall allows inbound TCP on ports 3000 and 3030.
